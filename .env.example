# Copiar a .env.local. La key publishable es la sb_publishable_..., no la anon legacy.
#
# El proyecto se llama «proyectos» y esta COMPARTIDO con job-hunter: el plan free
# permite solo 2 proyectos activos, asi que cada app es un prefijo de tabla
# (`finanzas_*` aca) dentro del mismo Postgres. El ref y estas dos variables no
# cambiaron con la fusion: el proyecto es el mismo de siempre, solo se renombro.
NEXT_PUBLIC_SUPABASE_URL=https://tqdjpnxidmypsrlnvmdv.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
